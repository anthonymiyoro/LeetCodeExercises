class Solution:
    def subarraysWithKDistinct(self, s, k):
        if K > len(s):
            return 0
        res = 0
        
        # Looping through string
        for i in range(len(s)):
            # Convert string to array of distinct values
            dist = set()
            for j in range(i, len(s))
            
            